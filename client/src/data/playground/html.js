export const htmlPlayground = {
  title: "The EduQuest HTML Codex",
  subtitle: "",
  livePreview: true,
  chapters: [
    {
      id: "html-foundation",
      title: "The Foundation",
      description: "Discover the ancient tags and build your first temple",
      totalXp: 300,
      problems: [
        {
          id: "html-f-1",
          title: "The First Incantation",
          difficulty: "Easy",
          xp: 50,
          isCompleted: false,
          description: `Create the DOCTYPE declaration at the beginning of your document. This tells the browser that you're writing HTML5.\n\nThen create a basic HTML structure with:\n- An \`<html>\` tag\n- A \`<head>\` tag with a \`<title>\` of "My Page"\n- A \`<body>\` tag with an \`<h1>\` saying "Hello World"`,
          hints: [
            "Start with <!DOCTYPE html>",
            "The basic structure is: html > head + body",
            "Put <title> inside <head>",
          ],
          starterCode: `<!-- Start your journey here... -->\n`,
          testFunction: `const html = doc.documentElement; const h1 = doc.querySelector('h1'); const title = doc.querySelector('title'); if (!html) return { success: false, message: "No <html> element found." }; if (!doc.head) return { success: false, message: "No <head> element found." }; if (!title || title.textContent !== "My Page") return { success: false, message: "Title should be 'My Page'." }; if (!doc.body) return { success: false, message: "No <body> element found." }; if (!h1 || h1.textContent !== "Hello World") return { success: false, message: "Need an <h1> with 'Hello World'." }; return { success: true, message: "Your first HTML page is alive!" };`,
        },
        {
          id: "html-f-2",
          title: "Heading Hierarchy",
          difficulty: "Easy",
          xp: 50,
          isCompleted: false,
          description: `Create a heading hierarchy using all six heading levels (h1 through h6). Each should contain the text "Heading X" where X is the level number.`,
          hints: ["Use <h1> through <h6>", "Example: <h1>Heading 1</h1>"],
          starterCode: `<!-- Create all 6 heading levels -->\n`,
          testFunction: `let errors = []; for (let i = 1; i <= 6; i++) { const h = doc.querySelector('h'+i); if (!h) errors.push('Missing <h'+i+'>'); else if (h.textContent !== 'Heading '+i) errors.push('<h'+i+'> should say "Heading '+i+'"'); } if (errors.length) return { success: false, message: errors.join('. ') }; return { success: true, message: "All heading levels mastered!" };`,
        },
        {
          id: "html-f-3",
          title: "Paragraphs & Line Breaks",
          difficulty: "Easy",
          xp: 50,
          isCompleted: false,
          description: `Create two paragraphs:\n- First: "The web is built with HTML."\n- Second: "CSS makes it beautiful."\n\nAdd a horizontal rule \`<hr>\` between them.`,
          hints: ["Use <p> for paragraphs", "<hr> creates a horizontal line"],
          starterCode: `<!-- Create paragraphs with a horizontal rule -->\n`,
          testFunction: `const ps = doc.querySelectorAll('p'); const hr = doc.querySelector('hr'); if (ps.length < 2) return { success: false, message: "Need at least 2 paragraphs." }; if (!hr) return { success: false, message: "Missing <hr> element." }; if (ps[0].textContent.trim() !== "The web is built with HTML.") return { success: false, message: "First paragraph text is wrong." }; if (ps[1].textContent.trim() !== "CSS makes it beautiful.") return { success: false, message: "Second paragraph text is wrong." }; return { success: true, message: "Paragraphs and rules mastered!" };`,
        },
        {
          id: "html-f-4",
          title: "Bold & Italic",
          difficulty: "Easy",
          xp: 50,
          isCompleted: false,
          description: `Create a paragraph that contains:\n- The word "bold" wrapped in a \`<strong>\` tag\n- The word "italic" wrapped in an \`<em>\` tag`,
          hints: ["<strong> makes text bold", "<em> makes text italic"],
          starterCode: `<p>\n  <!-- Add bold and italic text -->\n</p>\n`,
          testFunction: `const strong = doc.querySelector('strong'); const em = doc.querySelector('em'); if (!strong) return { success: false, message: "Missing <strong> element." }; if (!em) return { success: false, message: "Missing <em> element." }; if (!strong.textContent.includes('bold')) return { success: false, message: "<strong> should contain 'bold'." }; if (!em.textContent.includes('italic')) return { success: false, message: "<em> should contain 'italic'." }; return { success: true, message: "Text formatting complete!" };`,
        },
        {
          id: "html-f-5",
          title: "Comments",
          difficulty: "Easy",
          xp: 50,
          isCompleted: false,
          description: `Create an HTML comment that says "This is a comment" and below it a paragraph saying "This is visible".`,
          hints: [
            "Comments use <!-- text -->",
            "Comments are not displayed in the browser",
          ],
          starterCode: `<!-- Write your code here -->\n`,
          testFunction: `const p = doc.querySelector('p'); if (!p) return { success: false, message: "Missing <p> element." }; if (p.textContent.trim() !== "This is visible") return { success: false, message: "Paragraph should say 'This is visible'." }; return { success: true, message: "Comments understood!" };`,
        },
      ],
    },
    {
      id: "html-text",
      title: "The Text Scrolls",
      description:
        "Master the art of formatting text and creating readable documents",
      totalXp: 350,
      problems: [
        {
          id: "html-t-1",
          title: "Ordered Lists",
          difficulty: "Easy",
          xp: 50,
          isCompleted: false,
          description: `Create an ordered list (\`<ol>\`) with 3 items:\n1. "HTML"\n2. "CSS"\n3. "JavaScript"`,
          hints: ["Use <ol> for ordered list", "Each item uses <li>"],
          starterCode: `<!-- Create an ordered list -->\n`,
          testFunction: `const ol = doc.querySelector('ol'); if (!ol) return { success: false, message: "Missing <ol> element." }; const items = ol.querySelectorAll('li'); if (items.length !== 3) return { success: false, message: "Need exactly 3 list items." }; const expected = ["HTML","CSS","JavaScript"]; for (let i=0;i<3;i++) { if (items[i].textContent.trim() !== expected[i]) return { success: false, message: "Item "+(i+1)+" should be '"+expected[i]+"'." }; } return { success: true, message: "Ordered list created!" };`,
        },
        {
          id: "html-t-2",
          title: "Unordered Lists",
          difficulty: "Easy",
          xp: 50,
          isCompleted: false,
          description: `Create an unordered list (\`<ul>\`) of 3 fruits: "Apple", "Banana", "Cherry".`,
          hints: ["Use <ul> for bullet lists", "Each item is an <li>"],
          starterCode: `<!-- Create an unordered list -->\n`,
          testFunction: `const ul = doc.querySelector('ul'); if (!ul) return { success: false, message: "Missing <ul>." }; const items = ul.querySelectorAll('li'); if (items.length !== 3) return { success: false, message: "Need 3 items." }; const expected = ["Apple","Banana","Cherry"]; for (let i=0;i<3;i++) { if (items[i].textContent.trim() !== expected[i]) return { success: false, message: "Item "+(i+1)+" should be '"+expected[i]+"'." }; } return { success: true, message: "Unordered list done!" };`,
        },
        {
          id: "html-t-3",
          title: "Blockquotes",
          difficulty: "Easy",
          xp: 50,
          isCompleted: false,
          description: `Create a \`<blockquote>\` element containing the text: "The only way to learn programming is by writing code."`,
          hints: ["Use the <blockquote> tag"],
          starterCode: `<!-- Create a blockquote -->\n`,
          testFunction: `const bq = doc.querySelector('blockquote'); if (!bq) return { success: false, message: "Missing <blockquote>." }; if (!bq.textContent.includes("The only way to learn programming is by writing code")) return { success: false, message: "Blockquote text doesn't match." }; return { success: true, message: "Blockquote mastered!" };`,
        },
        {
          id: "html-t-4",
          title: "Code Elements",
          difficulty: "Medium",
          xp: 75,
          isCompleted: false,
          description: `Create a paragraph that says "Use the " followed by a \`<code>\` element containing "console.log()" followed by " function to debug."`,
          hints: ["<code> is for inline code", "Nest it inside a <p>"],
          starterCode: `<!-- Show inline code -->\n`,
          testFunction: `const code = doc.querySelector('code'); const p = doc.querySelector('p'); if (!p) return { success: false, message: "Missing <p>." }; if (!code) return { success: false, message: "Missing <code>." }; if (!code.textContent.includes("console.log()")) return { success: false, message: "<code> should contain 'console.log()'." }; return { success: true, message: "Code element works!" };`,
        },
        {
          id: "html-t-5",
          title: "Preformatted Text",
          difficulty: "Easy",
          xp: 50,
          isCompleted: false,
          description: `Use \`<pre>\` to display this ASCII art exactly as written:\n\`\`\`\n  /\\\\\n /  \\\\\n/____\\\\\n\`\`\``,
          hints: ["<pre> preserves whitespace and line breaks"],
          starterCode: `<!-- Use <pre> for preformatted text -->\n`,
          testFunction: `const pre = doc.querySelector('pre'); if (!pre) return { success: false, message: "Missing <pre> element." }; if (!pre.textContent.includes('/')) return { success: false, message: "<pre> should contain ASCII art." }; return { success: true, message: "Preformatted text works!" };`,
        },
        {
          id: "html-t-6",
          title: "Subscript & Superscript",
          difficulty: "Medium",
          xp: 75,
          isCompleted: false,
          description: `Create:\n- A paragraph showing "HΓééO" using \`<sub>\` (subscript for the 2)\n- A paragraph showing "E=mc┬▓" using \`<sup>\` (superscript for the 2)`,
          hints: ["<sub> for subscript", "<sup> for superscript"],
          starterCode: `<!-- Show subscript and superscript -->\n`,
          testFunction: `const sub = doc.querySelector('sub'); const sup = doc.querySelector('sup'); if (!sub) return { success: false, message: "Missing <sub>." }; if (!sup) return { success: false, message: "Missing <sup>." }; return { success: true, message: "Sub/superscript mastered!" };`,
        },
      ],
    },
    {
      id: "html-links",
      title: "The Linking Portals",
      description:
        "Open portals to other pages and navigate the digital universe",
      totalXp: 300,
      problems: [
        {
          id: "html-l-1",
          title: "Your First Link",
          difficulty: "Easy",
          xp: 50,
          isCompleted: false,
          description: `Create a link (\`<a>\`) that:\n- Has the text "Visit Google"\n- Points to "https://google.com"\n- Opens in a new tab (target="_blank")`,
          hints: [
            "Use <a href='url'>text</a>",
            "Add target='_blank' to open in new tab",
          ],
          starterCode: `<!-- Create a link -->\n`,
          testFunction: `const a = doc.querySelector('a'); if (!a) return { success: false, message: "Missing <a> element." }; if (a.textContent.trim() !== "Visit Google") return { success: false, message: "Link text should be 'Visit Google'." }; if (!a.href.includes("google.com")) return { success: false, message: "href should point to google.com." }; if (a.target !== "_blank") return { success: false, message: "Missing target='_blank'." }; return { success: true, message: "Link created!" };`,
        },
        {
          id: "html-l-2",
          title: "Adding Images",
          difficulty: "Easy",
          xp: 50,
          isCompleted: false,
          description: `Add an image (\`<img>\`) with:\n- src="https://via.placeholder.com/200"\n- alt="Placeholder Image"\n- width of 200`,
          hints: ["<img> is self-closing", "Always include an alt attribute"],
          starterCode: `<!-- Add an image -->\n`,
          testFunction: `const img = doc.querySelector('img'); if (!img) return { success: false, message: "Missing <img>." }; if (!img.alt || img.alt !== "Placeholder Image") return { success: false, message: "alt should be 'Placeholder Image'." }; if (!img.src.includes("placeholder")) return { success: false, message: "src should be the placeholder URL." }; return { success: true, message: "Image added!" };`,
        },
        {
          id: "html-l-3",
          title: "Navigation Menu",
          difficulty: "Medium",
          xp: 75,
          isCompleted: false,
          description: `Create a \`<nav>\` element containing an unordered list with 3 links:\n- "Home" linking to "#home"\n- "About" linking to "#about"\n- "Contact" linking to "#contact"`,
          hints: ["Wrap <ul> inside <nav>", "Each <li> contains an <a>"],
          starterCode: `<!-- Create a navigation menu -->\n`,
          testFunction: `const nav = doc.querySelector('nav'); if (!nav) return { success: false, message: "Missing <nav>." }; const links = nav.querySelectorAll('a'); if (links.length < 3) return { success: false, message: "Need at least 3 links in nav." }; const ul = nav.querySelector('ul'); if (!ul) return { success: false, message: "Links should be in a <ul>." }; return { success: true, message: "Navigation menu created!" };`,
        },
        {
          id: "html-l-4",
          title: "Email & Phone Links",
          difficulty: "Easy",
          xp: 50,
          isCompleted: false,
          description: `Create two links:\n- One with \`mailto:hello@example.com\` and text "Email Us"\n- One with \`tel:+1234567890\` and text "Call Us"`,
          hints: ["Use mailto: for email links", "Use tel: for phone links"],
          starterCode: `<!-- Create email and phone links -->\n`,
          testFunction: `const links = doc.querySelectorAll('a'); if (links.length < 2) return { success: false, message: "Need 2 links." }; let hasMailto = false, hasTel = false; links.forEach(a => { if (a.href.includes('mailto:')) hasMailto = true; if (a.href.includes('tel:')) hasTel = true; }); if (!hasMailto) return { success: false, message: "Missing mailto: link." }; if (!hasTel) return { success: false, message: "Missing tel: link." }; return { success: true, message: "Special links work!" };`,
        },
      ],
    },
    {
      id: "html-forms",
      title: "The Input Scrolls",
      description: "Master forms and capture user data",
      totalXp: 350,
      problems: [
        {
          id: "html-fo-1",
          title: "Basic Form",
          difficulty: "Medium",
          xp: 75,
          isCompleted: false,
          description: `Create a \`<form>\` with:\n- A text input with name="username" and placeholder="Enter username"\n- A password input with name="password"\n- A submit button with text "Login"`,
          hints: [
            "type='text' for text input",
            "type='password' for password",
            "type='submit' for button",
          ],
          starterCode: `<!-- Create a login form -->\n`,
          testFunction: `const form = doc.querySelector('form'); if (!form) return { success: false, message: "Missing <form>." }; const user = form.querySelector('input[name="username"]'); const pass = form.querySelector('input[name="password"]'); const btn = form.querySelector('[type="submit"], button'); if (!user) return { success: false, message: "Missing username input." }; if (!pass) return { success: false, message: "Missing password input." }; if (!btn) return { success: false, message: "Missing submit button." }; return { success: true, message: "Login form created!" };`,
        },
        {
          id: "html-fo-2",
          title: "Labels & Accessibility",
          difficulty: "Medium",
          xp: 75,
          isCompleted: false,
          description: `Create a form with a \`<label>\` and \`<input>\` pair. The label should say "Email:" and have a \`for\` attribute matching the input's \`id\` of "email". The input should be type="email".`,
          hints: [
            "<label for='id'> links to <input id='id'>",
            "This improves accessibility",
          ],
          starterCode: `<form>\n  <!-- Add label and input -->\n</form>\n`,
          testFunction: `const label = doc.querySelector('label'); const input = doc.querySelector('input[type="email"]'); if (!label) return { success: false, message: "Missing <label>." }; if (!input) return { success: false, message: "Missing email input." }; if (!input.id) return { success: false, message: "Input needs an id." }; if (label.htmlFor !== input.id) return { success: false, message: "Label 'for' must match input 'id'." }; return { success: true, message: "Accessible form!" };`,
        },
        {
          id: "html-fo-3",
          title: "Select Dropdown",
          difficulty: "Easy",
          xp: 50,
          isCompleted: false,
          description: `Create a \`<select>\` dropdown with name="color" and 3 options:\n- "Red" (value="red")\n- "Green" (value="green")\n- "Blue" (value="blue")`,
          hints: [
            "Use <select> with <option> children",
            "Each option has a value attribute",
          ],
          starterCode: `<!-- Create a dropdown -->\n`,
          testFunction: `const select = doc.querySelector('select'); if (!select) return { success: false, message: "Missing <select>." }; const opts = select.querySelectorAll('option'); if (opts.length < 3) return { success: false, message: "Need 3 options." }; return { success: true, message: "Dropdown created!" };`,
        },
        {
          id: "html-fo-4",
          title: "Table of Data",
          difficulty: "Medium",
          xp: 75,
          isCompleted: false,
          description: `Create a \`<table>\` with:\n- A \`<thead>\` row with headers: "Name", "Age"\n- A \`<tbody>\` with 2 rows of data:\n  - "Alice", "25"\n  - "Bob", "30"`,
          hints: [
            "Use <th> for header cells",
            "Use <td> for data cells",
            "Wrap header row in <thead>, data in <tbody>",
          ],
          starterCode: `<!-- Create a data table -->\n`,
          testFunction: `const table = doc.querySelector('table'); if (!table) return { success: false, message: "Missing <table>." }; const thead = table.querySelector('thead'); const tbody = table.querySelector('tbody'); if (!thead) return { success: false, message: "Missing <thead>." }; if (!tbody) return { success: false, message: "Missing <tbody>." }; const ths = thead.querySelectorAll('th'); if (ths.length < 2) return { success: false, message: "Need 2 header cells." }; const rows = tbody.querySelectorAll('tr'); if (rows.length < 2) return { success: false, message: "Need 2 data rows." }; return { success: true, message: "Table created!" };`,
        },
        {
          id: "html-fo-5",
          title: "Semantic HTML",
          difficulty: "Medium",
          xp: 75,
          isCompleted: false,
          description: `Build a semantic page structure using:\n- \`<header>\` with an \`<h1>\` saying "My Website"\n- \`<main>\` with a \`<p>\` saying "Welcome!"\n- \`<footer>\` with text "┬⌐ 2024"`,
          hints: [
            "Semantic tags describe their purpose",
            "<header>, <main>, <footer> structure a page",
          ],
          starterCode: `<!-- Build a semantic page -->\n`,
          testFunction: `const header = doc.querySelector('header'); const main = doc.querySelector('main'); const footer = doc.querySelector('footer'); if (!header) return { success: false, message: "Missing <header>." }; if (!main) return { success: false, message: "Missing <main>." }; if (!footer) return { success: false, message: "Missing <footer>." }; if (!header.querySelector('h1')) return { success: false, message: "<header> needs an <h1>." }; if (!main.querySelector('p')) return { success: false, message: "<main> needs a <p>." }; return { success: true, message: "Semantic HTML mastered!" };`,
        },
      ],
    },
    // ADVANCED HTML5 & MODERN WEB DEVELOPMENT
    {
      id: "html-multimedia",
      title: "The Multimedia Gallery",
      description: "Master audio, video, and interactive media elements",
      totalXp: 400,
      problems: [
        {
          id: "html-m-1",
          title: "Video Embedding",
          difficulty: "Medium",
          xp: 80,
          isCompleted: false,
          description: `Create a video element with:\n- src="https://www.w3schools.com/html/mov_bbb.mp4"\n- controls attribute\n- width="320"\n- height="240"\n- Fallback text "Your browser does not support the video tag."`,
          hints: [
            "Use <video> with controls",
            "Add fallback text inside video tag",
          ],
          starterCode: `<!-- Add a video element -->\n`,
          testFunction: `const video = doc.querySelector('video'); if (!video) return { success: false, message: "Missing <video> element." }; if (!video.hasAttribute('controls')) return { success: false, message: "Missing controls attribute." }; if (!video.textContent.includes("Your browser does not support")) return { success: false, message: "Missing fallback text." }; return { success: true, message: "Video element created!" };`,
        },
        {
          id: "html-m-2",
          title: "Audio Player",
          difficulty: "Medium",
          xp: 80,
          isCompleted: false,
          description: `Create an audio element with:\n- controls attribute\n- A source element pointing to "https://www.w3schools.com/html/horse.mp3"\n- Fallback text "Your browser does not support the audio element."`,
          hints: [
            "Use <audio> with <source> inside",
            "Source needs src and type attributes",
          ],
          starterCode: `<!-- Add an audio element -->\n`,
          testFunction: `const audio = doc.querySelector('audio'); const source = doc.querySelector('source'); if (!audio) return { success: false, message: "Missing <audio>." }; if (!source) return { success: false, message: "Missing <source>." }; if (!audio.hasAttribute('controls')) return { success: false, message: "Missing controls." }; return { success: true, message: "Audio element created!" };`,
        },
        {
          id: "html-m-3",
          title: "Canvas Drawing",
          difficulty: "Hard",
          xp: 120,
          isCompleted: false,
          description: `Create a canvas element with:\n- id="myCanvas"\n- width="200"\n- height="100"\n- A JavaScript function that draws a blue rectangle when the page loads`,
          hints: ["Use <canvas> with id", "Add script to get context and draw"],
          starterCode: `<canvas id="myCanvas" width="200" height="100"></canvas>\n<script>\n  // Draw a blue rectangle here\n</script>`,
          testFunction: `const canvas = doc.querySelector('canvas'); if (!canvas) return { success: false, message: "Missing <canvas>." }; if (canvas.id !== "myCanvas") return { success: false, message: "Canvas should have id 'myCanvas'." }; return { success: true, message: "Canvas element ready!" };`,
        },
        {
          id: "html-m-4",
          title: "SVG Graphics",
          difficulty: "Medium",
          xp: 120,
          isCompleted: false,
          description: `Create an SVG element with:\n- width="100" height="100"\n- A circle element with cx="50" cy="50" r="40" fill="red"`,
          hints: [
            "Use <svg> with namespace",
            "Circle needs cx, cy, r, and fill attributes",
          ],
          starterCode: `<!-- Create an SVG with a circle -->\n`,
          testFunction: `const svg = doc.querySelector('svg'); const circle = doc.querySelector('circle'); if (!svg) return { success: false, message: "Missing <svg>." }; if (!circle) return { success: false, message: "Missing <circle>." }; if (circle.getAttribute('fill') !== 'red') return { success: false, message: "Circle should be red." }; return { success: true, message: "SVG graphics created!" };`,
        },
      ],
    },
    {
      id: "html-apis",
      title: "The Web API Chronicles",
      description: "Harness the power of modern browser APIs and interactivity",
      totalXp: 500,
      problems: [
        {
          id: "html-api-1",
          title: "Data Attributes",
          difficulty: "Medium",
          xp: 100,
          isCompleted: false,
          description: `Create a div with:\n- class="user-card"\n- data-user-id="123"\n- data-role="admin"\n- data-last-login="2023-01-15"\n\nAdd a button that when clicked, displays these data attributes in an alert.`,
          hints: [
            "Use data-* attributes",
            "Access with dataset property in JavaScript",
          ],
          starterCode: `<div class="user-card">\n  <!-- Add data attributes -->\n  User Info\n</div>\n<button onclick="showUserInfo()">Show User Info</button>\n<script>\nfunction showUserInfo() {\n  // Display data attributes here\n}\n</script>`,
          testFunction: `const div = doc.querySelector('.user-card'); const button = doc.querySelector('button'); if (!div) return { success: false, message: "Missing user-card div." }; if (!button) return { success: false, message: "Missing button." }; return { success: true, message: "Data attributes setup complete!" };`,
        },
        {
          id: "html-api-2",
          title: "Local Storage Integration",
          difficulty: "Hard",
          xp: 150,
          isCompleted: false,
          description: `Create a todo list application that:\n- Has an input field with id="todoInput"\n- Has a button to add todos\n- Stores todos in localStorage\n- Loads todos on page load\n- Can mark todos as complete`,
          hints: [
            "Use localStorage.setItem/getItem",
            "Store as JSON string",
            "Parse on load",
          ],
          starterCode: `<input type="text" id="todoInput" placeholder="Enter todo...">\n<button onclick="addTodo()">Add Todo</button>\n<ul id="todoList"></ul>\n\n<script>\n// Todo functionality here\n</script>`,
          testFunction: `const input = doc.querySelector('#todoInput'); const button = doc.querySelector('button'); const list = doc.querySelector('#todoList'); if (!input || !button || !list) return { success: false, message: "Missing required elements." }; return { success: true, message: "Todo app structure ready!" };`,
        },
        {
          id: "html-api-3",
          title: "Geolocation API",
          difficulty: "Hard",
          xp: 150,
          isCompleted: false,
          description: `Create a button that:\n- Says "Get My Location"\n- When clicked, uses the Geolocation API\n- Displays latitude and longitude in a div with id="location"\n- Handles errors gracefully`,
          hints: [
            "Use navigator.geolocation.getCurrentPosition",
            "Handle success and error callbacks",
          ],
          starterCode: `<button onclick="getLocation()">Get My Location</button>\n<div id="location">Location will appear here</div>\n\n<script>\nfunction getLocation() {\n  // Implement geolocation here\n}\n</script>`,
          testFunction: `const button = doc.querySelector('button'); const locationDiv = doc.querySelector('#location'); if (!button || !locationDiv) return { success: false, message: "Missing required elements." }; return { success: true, message: "Geolocation setup complete!" };`,
        },
        {
          id: "html-api-4",
          title: "Drag and Drop",
          difficulty: "Hard",
          xp: 100,
          isCompleted: false,
          description: `Create a drag-and-drop interface with:\n- A draggable div with id="dragItem"\n- A drop zone with id="dropZone"\n- Visual feedback during dragging\n- Success message when dropped`,
          hints: [
            "Use draggable='true'",
            "Handle dragstart, dragover, drop events",
            "preventDefault in dragover",
          ],
          starterCode: `<div id="dragItem" draggable="true">Drag Me!</div>\n<div id="dropZone">Drop Here</div>\n\n<script>\n// Implement drag and drop\n</script>`,
          testFunction: `const dragItem = doc.querySelector('#dragItem'); const dropZone = doc.querySelector('#dropZone'); if (!dragItem || !dropZone) return { success: false, message: "Missing required elements." }; if (!dragItem.hasAttribute('draggable')) return { success: false, message: "Missing draggable attribute." }; return { success: true, message: "Drag and drop structure ready!" };`,
        },
      ],
    },
    {
      id: "html-performance",
      title: "The Performance Optimization",
      description: "Build lightning-fast, efficient web applications",
      totalXp: 450,
      problems: [
        {
          id: "html-perf-1",
          title: "Lazy Loading Images",
          difficulty: "Medium",
          xp: 100,
          isCompleted: false,
          description: `Create three images with:\n- loading="lazy" attribute\n- Meaningful alt text\n- Different placeholder URLs\n- One image with width/height attributes to prevent layout shift`,
          hints: [
            "Use loading='lazy' for offscreen images",
            "Always include alt text",
            "Add width/height to prevent CLS",
          ],
          starterCode: `<!-- Add lazy-loaded images -->\n`,
          testFunction: `const images = doc.querySelectorAll('img'); let lazyCount = 0; images.forEach(img => { if (img.getAttribute('loading') === 'lazy') lazyCount++; }); if (lazyCount < 3) return { success: false, message: "Need 3 lazy-loaded images." }; return { success: true, message: "Lazy loading implemented!" };`,
        },
        {
          id: "html-perf-2",
          title: "Resource Preloading",
          difficulty: "Hard",
          xp: 120,
          isCompleted: false,
          description: `Add to the head:\n- A preload link for a critical CSS file "styles.css"\n- A prefetch link for a future JavaScript file "future.js"\n- A dns-prefetch for an external API "https://api.example.com"`,
          hints: [
            "Use <link rel='preload'> for critical resources",
            "Use <link rel='prefetch'> for future resources",
            "Use <link rel='dns-prefetch'> for external domains",
          ],
          starterCode: `<head>\n  <!-- Add resource hints -->\n</head>`,
          testFunction: `const preload = doc.querySelector('link[rel="preload"]'); const prefetch = doc.querySelector('link[rel="prefetch"]'); const dnsPrefetch = doc.querySelector('link[rel="dns-prefetch"]'); if (!preload) return { success: false, message: "Missing preload link." }; if (!prefetch) return { success: false, message: "Missing prefetch link." }; if (!dnsPrefetch) return { success: false, message: "Missing dns-prefetch link." }; return { success: true, message: "Resource hints implemented!" };`,
        },
        {
          id: "html-perf-3",
          title: "Critical CSS Inlining",
          difficulty: "Hard",
          xp: 130,
          isCompleted: false,
          description: `Create a page with:\n- Critical CSS inlined in a <style> tag in the head\n- Non-critical CSS loaded asynchronously\n- Above-the-fold content that renders immediately`,
          hints: [
            "Inline critical CSS in <style>",
            "Load non-critical CSS with JavaScript",
            "Prioritize above-the-fold content",
          ],
          starterCode: `<head>\n  <style>\n    /* Add critical CSS here */\n  </style>\n</head>\n<body>\n  <!-- Above-the-fold content -->\n  <script>\n    // Load non-critical CSS here\n  </script>\n</body>`,
          testFunction: `const style = doc.querySelector('style'); const script = doc.querySelector('script'); if (!style) return { success: false, message: "Missing inline styles." }; return { success: true, message: "Critical CSS structure ready!" };`,
        },
        {
          id: "html-perf-4",
          title: "Image Optimization",
          difficulty: "Medium",
          xp: 100,
          isCompleted: false,
          description: `Create responsive images with:\n- A picture element with multiple source tags\n- Different image formats (WebP, JPEG)\n- Media queries for different screen sizes\n- Fallback img element`,
          hints: [
            "Use <picture> with <source> elements",
            "Include WebP format for modern browsers",
            "Add media queries for responsiveness",
          ],
          starterCode: `<picture>\n  <!-- Add responsive image sources -->\n</picture>`,
          testFunction: `const picture = doc.querySelector('picture'); const sources = picture.querySelectorAll('source'); const img = picture.querySelector('img'); if (!picture || !img) return { success: false, message: "Missing picture or img element." }; return { success: true, message: "Responsive images setup complete!" };`,
        },
      ],
    },
    {
      id: "html-accessibility",
      title: "The Accessibility Citadel",
      description: "Build inclusive web experiences for all users",
      totalXp: 400,
      problems: [
        {
          id: "html-a11y-1",
          title: "ARIA Labels",
          difficulty: "Medium",
          xp: 100,
          isCompleted: false,
          description: `Create a button with:\n- aria-label="Close dialog"\n- An icon (X) but no visible text\n- Proper semantic meaning for screen readers`,
          hints: [
            "Use aria-label for buttons with only icons",
            "Ensure buttons have accessible names",
            "Test with screen readers",
          ],
          starterCode: `<button><!-- Add icon and aria-label -->X</button>`,
          testFunction: `const button = doc.querySelector('button'); if (!button) return { success: false, message: "Missing button." }; if (!button.hasAttribute('aria-label')) return { success: false, message: "Missing aria-label." }; return { success: true, message: "ARIA label added!" };`,
        },
        {
          id: "html-a11y-2",
          title: "Semantic Forms",
          difficulty: "Medium",
          xp: 100,
          isCompleted: false,
          description: `Create an accessible form with:\n- Proper label elements linked to inputs\n- Fieldset and legend for related groups\n- Required attributes where appropriate\n- Error messages with aria-live`,
          hints: [
            "Use <label for='id'> for form inputs",
            "Group related fields with <fieldset>",
            "Use aria-live for dynamic error messages",
          ],
          starterCode: `<form>\n  <!-- Create accessible form fields -->\n</form>`,
          testFunction: `const form = doc.querySelector('form'); const labels = form.querySelectorAll('label'); const inputs = form.querySelectorAll('input'); if (!form || labels.length === 0 || inputs.length === 0) return { success: false, message: "Form needs labels and inputs." }; return { success: true, message: "Accessible form structure ready!" };`,
        },
        {
          id: "html-a11y-3",
          title: "Skip Links",
          difficulty: "Easy",
          xp: 80,
          isCompleted: false,
          description: `Create a skip link that:\n- Appears as the first focusable element\n- Links to the main content area\n- Is visually hidden until focused\n- Uses proper semantic markup`,
          hints: [
            "Use skip links for keyboard navigation",
            "Hide visually but keep accessible",
            "Link to main content id",
          ],
          starterCode: `<!-- Add skip link -->\n<header>Header content</header>\n<main id="main-content">Main content</main>`,
          testFunction: `const main = doc.querySelector('#main-content'); if (!main) return { success: false, message: "Missing main content with id." }; return { success: true, message: "Skip link target ready!" };`,
        },
        {
          id: "html-a11y-4",
          title: "Color Contrast",
          difficulty: "Easy",
          xp: 120,
          isCompleted: false,
          description: `Create buttons with:\n- Sufficient color contrast ratios\n- Not relying on color alone for meaning\n- Clear focus states\n- Accessible hover states`,
          hints: [
            "Ensure 4.5:1 contrast for normal text",
            "Don't use color as only indicator",
            "Add visible focus outlines",
          ],
          starterCode: `<style>\n  /* Add accessible button styles */\n</style>\n<button>Accessible Button</button>`,
          testFunction: `const button = doc.querySelector('button'); const style = doc.querySelector('style'); if (!button || !style) return { success: false, message: "Missing button or styles." }; return { success: true, message: "Accessibility styling setup!" };`,
        },
      ],
    },
    {
      id: "html-modern",
      title: "The Modern Web Frontier",
      description: "Explore cutting-edge web technologies and future standards",
      totalXp: 500,
      problems: [
        {
          id: "html-modern-1",
          title: "Web Components",
          difficulty: "Hard",
          xp: 150,
          isCompleted: false,
          description: `Create a custom web component:\n- Define a custom element class\n- Use shadow DOM\n- Add custom attributes\n- Make it reusable with slots`,
          hints: [
            "Use class extends HTMLElement",
            "Attach shadow DOM",
            "Define customElements.define()",
            "Use <slot> for content",
          ],
          starterCode: `<script>\n  // Define your custom component here\n</script>\n\n<!-- Use your custom component -->\n<my-component>Content</my-component>`,
          testFunction: `const script = doc.querySelector('script'); const component = doc.querySelector('my-component'); if (!script || !component) return { success: false, message: "Missing script or custom element." }; return { success: true, message: "Web component structure ready!" };`,
        },
        {
          id: "html-modern-2",
          title: "Progressive Web App",
          difficulty: "Hard",
          xp: 150,
          isCompleted: false,
          description: `Create PWA features:\n- Add a web app manifest link\n- Include service worker registration\n- Make it installable\n- Add offline functionality indicators`,
          hints: [
            "Use <link rel='manifest'>",
            "Register service worker in JavaScript",
            "Design for offline-first",
          ],
          starterCode: `<head>\n  <!-- Add PWA features -->\n</head>\n<script>\n  // Register service worker\n</script>`,
          testFunction: `const head = doc.querySelector('head'); const script = doc.querySelector('script'); if (!head || !script) return { success: false, message: "Missing PWA setup elements." }; return { success: true, message: "PWA structure initialized!" };`,
        },
        {
          id: "html-modern-3",
          title: "WebAssembly Integration",
          difficulty: "Expert",
          xp: 200,
          isCompleted: false,
          description: `Integrate WebAssembly:\n- Load a WASM module\n- Call WASM functions from JavaScript\n- Handle WASM errors gracefully\n- Show performance benefits`,
          hints: [
            "Use WebAssembly.instantiate()",
            "Handle promises properly",
            "Validate WASM support",
          ],
          starterCode: `<script>\n  // Load and use WebAssembly module\n</script>\n<div id="wasm-output">WASM results will appear here</div>`,
          testFunction: `const script = doc.querySelector('script'); const output = doc.querySelector('#wasm-output'); if (!script || !output) return { success: false, message: "Missing WASM setup elements." }; return { success: true, message: "WebAssembly structure ready!" };`,
        },
      ],
    },
  ],
};
